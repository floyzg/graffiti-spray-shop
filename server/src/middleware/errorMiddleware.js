/**
 * Error-handling middleware for Express.
 *
 * Что будет разрабатываться в этом модуле:
 * - Унифицированная обработка ошибок API (логирование, формирование ответа).
 * - Обработка 404 (route not found) и кастомных ошибок приложения.
 * - Форматирование ответа с полем `message` и, при необходимости, `details`.
 * - Интеграция с внешними логгерами при необходимости (winston, pino).
 *
 * TODO: экспортировать middleware-функцию `errorHandler(err, req, res, next)`.
 */


