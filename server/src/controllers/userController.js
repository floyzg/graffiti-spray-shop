/**
 * Контроллеры для работы с сущностью `User`.
 *
 * Что будет разрабатываться в этом модуле:
 * - Обработчики маршрутов для регистрации, входа (login) и выхода (logout).
 * - CRUD-операции для пользователя (получение профиля, обновление, удаление).
 * - Валидация входящих данных и возврат корректных HTTP-статусов.
 * - Взаимодействие с моделью `User` и сервисами аутентификации (JWT, хеш пароля).
 *
 * TODO: реализовать `register`, `login`, `getProfile`, `updateProfile`, `deleteUser`.
 */


