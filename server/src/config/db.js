/**
 * Database configuration and connection helper.
 *
 * Что будет разрабатываться в этом модуле:
 * - Чтение строки подключения из переменных окружения.
 * - Установка соединения с БД (например, MongoDB через mongoose).
 * - Обработка событий подключения (connected, error, disconnected).
 * - Экспорт функции/объекта для инициализации соединения из `index.js`.
 *
 * TODO: реализовать функцию `connectDB()` и добавить логирование ошибок.
 */


